{
  "name": "acme-corporation-website",
  "version": "0.1.0",
  "devDependencies": {
    "@easylogic/editor": "0.10.0"
  },
  "dependencies": {
    "mathjax": "3.1.2",
    "node-fetch": "2.6.1",
    "stripe": "^8.129.0"
  },
  "main": "api.js",
  "NOTE": "postinstall script added early for multi-branch checkout. Will be needed in the next checkpoint where this comment will be removed",
  "scripts": {
    "start": "node api.js",
    "postinstall": "node api/postinstall.js"
  }
}
